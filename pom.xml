<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.5</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>com.jw</groupId>
	<artifactId>holiday-guard</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>Holiday Guard</name>
	<description>A REST service for managing holidays and business days</description>
	<url>https://github.com/jw1/holiday-guard</url>

	<licenses>
		<license>
			<name>MIT License</name>
			<url>https://opensource.org/licenses/MIT</url>
		</license>
	</licenses>

	<developers>
		<developer>
			<id>jw1</id>
			<name>James Warlick</name>
			<url>https://github.com/jw1</url>
		</developer>
	</developers>

	<scm>
		<connection>scm:git:git://github.com/jw1/holiday-guard.git</connection>
		<developerConnection>scm:git:ssh://github.com/jw1/holiday-guard.git</developerConnection>
		<tag>HEAD</tag>
		<url>https://github.com/jw1/holiday-guard</url>
	</scm>

	<modules>
		<module>holiday-guard-domain</module>
		<module>holiday-guard-repository</module>
		<module>holiday-guard-core</module>
	</modules>

	<properties>
		<java.version>21</java.version>
		<!-- Shared JVM args for tests (Surefire, Failsafe, etc.) -->
		<argLine>
			-XX:+EnableDynamicAgentLoading
			--add-opens java.base/java.lang=ALL-UNNAMED
			--add-opens java.base/java.util=ALL-UNNAMED
		</argLine>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- Internal module dependencies -->
			<dependency>
				<groupId>com.jw</groupId>
				<artifactId>holiday-guard-domain</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.jw</groupId>
				<artifactId>holiday-guard-repository</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.jw</groupId>
				<artifactId>holiday-guard-core</artifactId>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>
				<!-- javac will complain about annotation processing, so enable for lombok -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<configuration>
						<compilerArgs>
							<arg>-proc:full</arg>
						</compilerArgs>
						<annotationProcessorPaths>
							<path>
								<groupId>org.projectlombok</groupId>
								<artifactId>lombok</artifactId>
								<version>${lombok.version}</version>
							</path>
						</annotationProcessorPaths>
					</configuration>
				</plugin>

				<!-- Unit & integration test plugins will both pick up argLine -->
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
				</plugin>

				<plugin>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-maven-plugin</artifactId>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>
